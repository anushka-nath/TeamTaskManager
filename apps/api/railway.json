{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build --workspace=@ttm/shared && npm run build --workspace=@ttm/api"
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy --schema=apps/api/prisma/schema.prisma && npm start"
  }
}

