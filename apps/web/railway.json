{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "npm run build --workspace=@ttm/shared && npm run build --workspace=@ttm/web"
  },
  "deploy": {
    "startCommand": "npx serve dist -s -l 80"
  }
}
